# 0xPR0MPT — AI/ML Red-Team Corpus Generator
#
# INSTALLATION TIERS
# ==================
# Corpus generation only (stable surface — `corpus` and `report` subcommands):
#   pip install requests
#
# Full install including experimental attack modules (`scan`, `supply-chain`):
#   pip install -r requirements.txt
#
# Note: numpy is NOT required for corpus generation or report generation.
# It is only needed by the experimental adversarial, model-extraction,
# data-poisoning, and inference modules.

# ---------------------------------------------------------------------------
# Required for corpus generation and report generation (stable surface)
# ---------------------------------------------------------------------------
requests>=2.31.0,<3.0

# ---------------------------------------------------------------------------
# Required for experimental modules (adversarial, extraction, poisoning,
# inference) — not needed for pure corpus generation
# ---------------------------------------------------------------------------
numpy>=1.24.0,<3.0

# Optional: Advanced ML attacks (uncomment as needed)
# torch>=1.10.0
# tensorflow>=2.7.0
# foolbox>=3.3.0
# adversarial-robustness-toolbox>=1.10.0

# Optional: LLM testing
# openai>=1.0.0
# anthropic>=0.5.0
# tiktoken>=0.4.0

# Optional: Security scanning
# safety>=2.0.0
# pip-audit>=2.0.0
# bandit>=1.7.0

# Optional: Reporting
# markdown>=3.4.0
# jinja2>=3.0.0
